#ifndef EXTENSION_H
#define EXTENSION_H

#include <string>

//parses one line of code from an .asm file, converting each command to it's respective machine code representation
std::string getFileExtension(char* filename);


#endif